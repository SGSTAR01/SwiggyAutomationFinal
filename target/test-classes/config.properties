browser=chrome
headless=false
url=https://www.swiggy.com

# Explicit wait (seconds) used by WaitUtils when not providing a Duration
explicit.wait=10

# Cart-specific settings
cart.retry.count=3
cart.wait.timeout=10

# Logging level (for reference; edit log4j2.xml to change active level)
logging.level=INFO
